<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {# asset permet de faire référence aux dossiers public de notre projet symfony #}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
            <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% endblock %}

 
    </head>
    <body>

        <nav>
            <ul>
                {# Permet d'afficher un lien menant vers la liste des entreprises #}
                <li><a href="{{ path('app_entreprise') }}">Liste des entreprises</a></li>

                {# Permet d'afficher un lien menant vers la liste des employés#}
                <li><a href="{{ path('app_employe') }}">Liste des employés</a></li>
            </ul>
        </nav>

        {% block body %}{% endblock %}

        {% block javascripts %}
            <script src="{{ asset('js/script.js') }}"></script>
        {% endblock %}
    </body>
</html>
